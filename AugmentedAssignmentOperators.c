#include <stdio.h>

int main(){

    /*Augmented Assignment Operators = used to replace a statement 
                                    where an operator takes a variable 
                                    as one of its arguments 
                                    and then assigns the result back to 
                                    the same variable.
    That is: for example, x+=1 or x = x + 1*/
    int x = 10;
    x += 2; // Here the variable x does not require any prefixions
}